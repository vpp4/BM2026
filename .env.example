# Copy to .env and paste your key in. .env is gitignored — never commit it.
# Request a key at https://api.burningman.org/request
BM_API_KEY=

# Optional: which year to pull (default 2026). Events go back to 2009.
BM_YEAR=2026
